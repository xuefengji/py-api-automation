# 测试项目信息
project_name: xxx 项目测试

# 测试执行人员
tester: 小小月芽儿

# 环境信息：测试、正式、预发布地址
env: 127.0.0.1

# 测试报告通知类型：0表示默认邮件、1表示钉钉
notification_type: 0

# 缓存机制：local表示本地存储，redis表示redis存储
cache: local

# mysql 连接信息
mysql:
  host: '127.0.0.1',  # MySQL服务端的IP地址
  port: 3306,  # MySQL默认PORT地址(端口号)
  user: 'root',  # 用户名
  password: '123456',  # 密码,也可以简写为passwd
  database: 'test',  # 库名称,也可以简写为db
  charset: 'utf8'


# Email 信息
email:
  host: smtp.qq.com     #邮箱服务
  user: 123456@qq.com   # 邮箱用户名
  key: qweeret          # 邮箱鉴权 key
  recevies:             # 收件人，可配置多个收件人
    - 12345678@qq.com

# 钉钉通知配置
ding_talk:
  webhook:
  secret:

